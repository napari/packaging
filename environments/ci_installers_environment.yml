name: build-bundle
channels:
  - napari/label/bundle_tools_3
  - conda-forge
dependencies:
  - python
  - pip
  - constructor
  - conda-build
  - ruamel.yaml
  - conda-standalone
  - conda
  - conda-libmamba-solver
  # only needed to import napari and get the path to the logo.png file
  - lazy_loader >=0.2
  - importlib_resources  # needed for py38 on windows; can be dropped when we use py39 there
